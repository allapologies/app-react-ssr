import React from 'react';

const Home = () => (
    <div>
        <div>Home component</div>
        <button onClick={()=> console.log('CLICKED')}>Click me</button>
    </div>
);

export default Home;
